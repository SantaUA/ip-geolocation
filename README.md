# React + TypeScript + Vite

this is home assigment of creating IP geolocation app

React + typescript + vite were used as core for application

To run application:

- node 22+ required
- run `npm i`
- to start application locally `npm run dev`